# Vicissitude Agent Instructions

## 基本方針

- 日本語で応答する。
- ユーザーの要求・要件が不明瞭な場合は掘り下げ、不明瞭な点を解消してから進める。
- 変更前に関連する仕様・ドキュメント・既存実装を確認し、作業計画を立てる。
- 実装は YAGNI / KISS / DRY を優先し、不要な後方互換パスやフォールバックを追加しない。
- TypeScript / JavaScript プロジェクトのスクリプト実行は `nr` を使う。ただし `bun test` など Bun の予約コマンドを直接使う必要がある場合は例外とする。

## プロジェクト概要

Discord bot「ふあ(hua)」のプロジェクト。Bun, TypeScript, OpenCode で動作する。

## ドキュメント導線

1. `README.md`: 要件定義・仕様

## コマンド

- `nr lint:fix` — Lint 自動修正 (`oxlint --fix`)
- `nr fmt` — フォーマット (`oxfmt`)
- `nr validate` — fmt:check + lint + check を一括実行
- `nr test` — 全テスト実行
- `nr test:spec` — 仕様テスト (`*.spec.ts`) のみ実行
- `nr test:unit` — ユニットテスト (`*.test.ts`) のみ実行
- `nr auto-triage:codex` — Codex 版 auto-triage ループ
- `nr auto-triage:codex:once` — Codex 版 auto-triage を 1 回だけ実行
- `nr auto-triage:claude` — Claude 版 auto-triage ループ
- `nr auto-triage:claude:once` — Claude 版 auto-triage を 1 回だけ実行
- `nr deploy` — コンテナを再作成してデプロイ（install → build → 起動）

## デプロイ

- デプロイ前に `hostname` でホストが `Feixiao` であることを確認する。`Feixiao` 以外ではデプロイしない。
- ソースコード・依存変更後: `nr deploy` で再デプロイする。

## 計画フェーズ

ユーザーから新しいタスク・機能要求・バグ報告を受けたとき、いきなり実行に入らず、まずタスク分解とスコープ特定を行う。

計画フェーズの責務は以下の 2 つだけ:

1. **タスク分解** — 何をどの順番でやるか
2. **スコープ特定** — どのファイル・仕様が関係するか

設計判断（インターフェースをどう切るか）やリスク判断（既存のどこが壊れそうか）は計画フェーズの責務外。計画後、利用可能な環境では以下の役割サブエージェントへ委譲する:

- **設計判断** → `spec-agent`
- **リスク判断** → `verify-agent`

## 実行フェーズ（役割サブエージェント）

Claude 環境では、4 つの役割を専用サブエージェント（`.claude/agents/` 配下に定義）として `Agent` ツールで起動する。各サブエージェントは tools フィールドで使用可能ツールが制限されており、`AskUserQuestion` は使えない。

| サブエージェント | 責務                                                       |
| ---------------- | ---------------------------------------------------------- |
| `spec-agent`     | 仕様策定（設計判断）、IF・型設計、仕様テスト、仕様書メンテ |
| `impl-agent`     | 仕様を忠実にコードへ落とし込む                             |
| `test-agent`     | 実装コードに対するユニットテスト作成                       |
| `verify-agent`   | テスト実行・型チェック・lint・仕様整合性の検証、リスク判断 |

呼び出し例: `Agent(subagent_type: "spec-agent", description: "...", prompt: "...")`

Codex 環境では、利用可能なサブエージェント・スキル・メインエージェント作業へ置き換えてもよいが、役割境界と情報境界は維持する。

### タスク別の呼び出しパターン

各パターンの前に、必ず計画フェーズでタスク分解・スコープ特定を行う。その後、以下のように役割サブエージェントへ順次委譲する。

**新機能追加:**

1. `spec-agent` → 設計判断 + 仕様テストを書く
2. `verify-agent` → リスク判断
3. `impl-agent` → spec だけ渡して実装させる
4. `test-agent` → unit テストを書く
5. `verify-agent` → 全テスト通るか確認

**バグ修正:**

1. `spec-agent` → 再現テスト（spec）を書く
2. `impl-agent` → 再現テストだけ渡して修正させる
3. `verify-agent` → 既存 spec が壊れてないか確認

**リファクタ:**

1. `verify-agent` → 既存 spec を確認 + リスク判断
2. `impl-agent` → spec だけ渡してリファクタさせる（unit テストは渡さない）
3. `verify-agent` → spec が通るか確認

リファクタ時に unit テストを渡さない。渡すと実装詳細に縛られる。

### テストファイルの分類

仕様テストとユニットテストはファイル拡張子で区別する。

| 種別           | ファイル名  | 書く人       | 目的                                                                          |
| -------------- | ----------- | ------------ | ----------------------------------------------------------------------------- |
| 仕様テスト     | `*.spec.ts` | `spec-agent` | 公開 API の振る舞い・契約を検証。実装詳細に依存しない（ブラックボックス）     |
| ユニットテスト | `*.test.ts` | `test-agent` | 内部ロジック・分岐・エッジケースを検証。実装に密結合で OK（ホワイトボックス） |

**配置ルール:**

- 仕様テスト (`*.spec.ts`): トップレベルの `spec/` ディレクトリに、`src/` のディレクトリ構造をミラーして配置する。
- ユニットテスト (`*.test.ts`): ソースファイルと同じディレクトリに co-locate する。

```text
spec/agent/
  session-store.spec.ts     # 仕様テスト（公開 API 契約）

src/agent/
  session-store.ts          # 実装
  session-store.test.ts     # ユニットテスト（内部ロジック詳細）
```

**境界ルール:**

| 観点                 | `*.spec.ts`                  | `*.test.ts`                    |
| -------------------- | ---------------------------- | ------------------------------ |
| テスト対象           | 公開インターフェース・ポート | 内部関数・プライベートロジック |
| モック対象           | 外部依存（DB, API 等）のみ   | 内部依存もモック可             |
| リファクタで壊れるか | 壊れてはいけない             | 壊れてよい                     |
| 書くタイミング       | 実装前（TDD）                | 実装後                         |

### サブエージェントの情報境界

各役割のサブエージェントを呼び出すときは、プロンプトに含める情報を以下のように制限する。情報を渡しすぎると役割の独立性が崩れる。

| サブエージェント | プロンプトに含めるもの                  | プロンプトに含めないもの        |
| ---------------- | --------------------------------------- | ------------------------------- |
| `spec-agent`     | タスク説明・既存 `*.spec.ts`・Plan 出力 | 実装コード・`*.test.ts`         |
| `impl-agent`     | `*.spec.ts`・型定義・スコープ           | `*.test.ts`・他モジュールの実装 |
| `test-agent`     | 実装コード・`*.spec.ts`                 | 他モジュール                    |
| `verify-agent`   | `*.spec.ts` のパス・実行結果・Plan 出力 | 実装の中身                      |

### メインエージェントの委譲ルール

- 役割サブエージェント（`spec-agent` / `impl-agent` / `test-agent` / `verify-agent`）の責務領域に踏み込まず、利用可能な環境では必ず役割サブエージェントへ委譲する。メインエージェントが自分で仕様テスト・実装・ユニットテスト・検証を代行しない。
- メインエージェントの責務は **計画・委譲・統合** であり、サブエージェントの作業を自ら代行することではない。
- サブエージェントは `AskUserQuestion` を持たないため、判断に迷う点は「メインエージェントへの確認事項」として返してくる。受け取ったらユーザーに掘り下げてから次のサブエージェントを起動する。
- サブエージェントはブロッキング呼び出しのため、対話モード・非対話モード（`-p` フラグ）の双方で同じ手順が使える。

## Issue 運用ルール

- 作業中に課題・改善点・技術的負債を発見したら、その場で GitHub Issue を立てる。後回しにしない。
- `/review-pr` 時に指摘した項目のうち、今回の PR で修正しなかったものは必ず Issue に残す。
- Issue はまとめすぎず、トピックごとに小分けにする（1 Issue = 1 トピック）。
- 重要な判断が必要な課題（設計方針の決定、破壊的変更の検討など）には `help wanted` ラベルを付ける。

## Git と PR

- main ブランチに直接コミットしない。
- 専用 worktree または作業ブランチで作業している場合は、そのブランチをそのまま使う。
- 作業完了後の PR 作成は確認不要。push してそのまま PR を作成する。
- コミットメッセージは Conventional Commits 形式で書く。要約は日本語にする。
  - 形式: `type(scope): 日本語の要約`
  - `scope` は必要な場合のみ付ける。
  - 例: `fix(agents): コミット規約を明確化する`
- 変更後は検証を実行し、成功したら必ずコミットする。
- コミット後は必ず push し、プルリクエストを作成する。
- コミット・push・PR 作成・マージの前に、追加の確認待ちで停止しない。
- PR 作成後は、指示されたワークフローでマージまで行う。auto-triage では `.agents/skills/auto-triage/SKILL.md` に従い、スカッシュマージしてリモートブランチを削除する。
- マージ前には必ず `review-pr` スキルを使用し、指摘は即修正または GitHub Issue 起票に振り分ける。未処理の指摘を残したままマージしない。
- マージ時はスカッシュマージし、マージ後にリモートブランチを削除する: `gh pr merge <number> --squash --delete-branch`

## auto-triage

- `auto-triage` 実行時は `.agents/skills/auto-triage/SKILL.md` を必ず読む。
- 1 回の実行で処理する GitHub Issue または main CI 失敗は 1 件だけにする。
- main CI 失敗がある場合は Issue より先に処理する。
- `help wanted` ラベル付き Issue は対象から除外する。
- 高レベル設計判断が必要な Issue は、理由をコメントして `help wanted` を付け、別の Issue を選ぶ。
- サブエージェントはコミットしない。コミット、push、PR 作成、マージは auto-triage を実行しているエージェント自身が行う。
- 完了前に `nr validate` と `nr test` を実行し、結果を報告に含める。

## 完了宣言ルール

- 「完了」「修正済み」「テスト通過」を主張するには、**そのメッセージ内で実行した検証証拠が必須**。
- 前回の実行結果や「通るはず」は証拠にならない。
- `impl-agent` は実装報告時に `nr check` / `nr lint` の実行結果を含める。
- `verify-agent` の検証は省略不可（「簡単な変更だから不要」は禁止）。
- 実行できなかった検証がある場合は、理由を明記する。
