# PLAN.md

## 1. 方針

- OpenCode SDK を AI ランタイムとして使用。責務別フラットモジュール構成（Pure DI）。
- 永続化は SQLite（Drizzle ORM）に統一。MCP サーバーは 4 プロセス構成。
- 詳細なアーキテクチャは `ARCHITECTURE.md` を参照。
- 完了済みマイルストーン（M1-M14a）の記録は `CHANGELOG.md` を参照。

## 2. 完了定義（DoD）

- `nr validate` が通る
- `bun test` が通る
- ドキュメント（ARCHITECTURE.md, STATUS.md）が現在の実装と一致する
- 手動動作確認: Discord 応答、Heartbeat 自律行動、Minecraft 接続（MC_HOST 設定時）
- 旧ディレクトリ・旧ファイルが `src/` に残っていない
- PR は各マイルストーン単位でマージする

## 3. 次のマイルストーン

### M13e. 正式アカウントログイン対応

目的:

- `auth: "offline"` 前提をやめ、必要に応じて Microsoft アカウントの正式ログインを選べるようにする。
- ローカル検証用の offline 接続と、本番用の正式アカウント接続を共存させる。

検討項目:

- 環境変数設計: `MC_AUTH_MODE`（例: `offline` / `microsoft`）、必要なら `MC_USERNAME` の役割見直し。
- mineflayer の Microsoft 認証フローに必要な資格情報保管方法。
- トークンキャッシュの配置先、失効時の再認証手順、RUNBOOK への反映。
- 2FA / デバイスコード / ブラウザ認証など、常駐プロセス向けに破綻しない運用手順。
- 認証失敗時に bot が無限再接続しないようにする制御。
- viewer や metrics など周辺機能が auth mode 差分で壊れないことの確認。

非目標:

- この段階では複数 Minecraft アカウントの同時運用までは広げない。
- 認証基盤を一般化して Discord や OpenCode 側に波及させない。

DoD:

- offline / microsoft の 2 モードが設定として整理され、秘密情報の置き場が決まる。
- `RUNBOOK.md` に再現可能なログイン手順を記述できる粒度まで設計が固まる。

### M13f. Discord 連携の高度化

- Discord からの自然言語依頼を、そのまま丸投げせず「依頼解釈 -> 計画 -> 実行可能タスク」へ変換する。
- 途中経過の共有を「重要時のみ」「うるさすぎない」形に調整する。
- `minecraft_status()` を単なる要約でなく、「現在の目標」「危険度」「実行中タスク」「詰まり理由」込みへ強化する。

DoD:

- ユーザー依頼、Minecraft 自律目標、緊急回避の優先順位が仕様化される。
- Discord 側から見た Minecraft エージェントの説明責任が改善される。

### M13g. 観測性・評価・運用フェーズ

- Minecraft エージェントの評価軸を「生存時間」「依頼完了率」「危険回避率」「目標進捗率」「無駄行動率」で定義する。
- 代表シナリオを固定化して回帰確認できるようにする。

候補シナリオ:

- 夜になったので安全に就寝または退避できる
- 空腹時に食料調達または食事できる
- プレイヤーからの採集依頼を受けて実行状況を返せる
- 危険 mob 接近時に無理な戦闘へ入らない
- 認証失敗時に暴走せず停止・通報できる

DoD:

- 追加すべきメトリクス、ログ、手動確認シナリオが列挙される。
- 「改善したつもり」を避けるための比較基準が決まる。

### M14b. ファクト注入の関連性フィルタ

現状: `ContextBuilder.loadLtmFacts()` が `getFacts()` で有効ファクト全件を取得し、システムプロンプトに注入している。ファクト数が増えるとコンテキストウィンドウを圧迫し、関連性の低いファクトがノイズになる。

対応:

- ファクト注入時に Retrieval のハイブリッド検索を使い、直近の会話コンテキストに関連するファクトのみを注入する
- 全件注入から「関連上位 N 件 + カテゴリ別上位」方式に変更する
- `LtmFactReader` インターフェースに `getRelevantFacts(guildId, context, limit)` を追加する

DoD:

- ファクト 100 件以上の環境で、注入されるファクトが関連性でフィルタされる
- システムプロンプトの LTM セクションサイズが制御可能になる

### M14c. 記憶システム責務統合（SPEC Phase 2-3）

現状: SPEC.md §3.6 のマイグレーション Phase 1（ファクト注入）は完了。Phase 2（MEMORY.md スリム化）と Phase 3（日次ログ再設計 + LESSONS.md 整理）が未着手。3 系統の記憶（LTM, MEMORY.md/LESSONS.md, 日次ログ）が独立して運用されており、情報の重複・散逸が発生する。

Phase 2 対応:

- MEMORY.md からユーザー情報（名前、特徴、関係性）を除去し、LTM SemanticFact に委譲する
- MEMORY.md を運用特化（チャンネル設定、行動ルール、週次目標）に限定する
- 数日の観察期間を設け、情報ロスがないことを確認する

Phase 3 対応:

- 日次ログの Heartbeat 実行記録を LTM Episode として自動取り込みする仕組みを検討する
- LESSONS.md の精選教訓と LTM の `guideline` カテゴリの責務を整理する
- 日次ログの保持期間と LTM への移行タイミングを定義する

DoD:

- MEMORY.md がスリム化され、ユーザー情報は LTM のみに存在する
- 日次ログから LTM への情報移行方針が決まる
- 各記憶システムの責務が重複しない

### M14d. Embedding 堅牢化

現状: embedding モデル（デフォルト: embeddinggemma, 384 次元）の次元数がコード内に定義されておらず、モデル変更時に保存済みデータと新規データの次元がズレてコサイン類似度が壊れる。

対応:

- LtmStorage に embedding 次元メタデータテーブルを追加し、初回保存時に次元を記録する
- 次元不一致を検出した場合にエラーまたは警告を出す
- モデル変更時の再 embedding マイグレーション手順を RUNBOOK.md に記載する

DoD:

- embedding 次元の不一致が検出可能になる
- モデル変更手順が RUNBOOK.md に記載される

### M14e. RAG パターン導入（将来検討）

現状: `ltm_retrieve` は MCP ツールとして AI エージェントが明示的に呼ぶ必要がある。新着メッセージに基づいて関連エピソードを自動検索し、コンテキストに注入する RAG パターンが未実装。

検討項目:

- イベントバッファのメッセージ内容に基づく自動 retrieve トリガー
- retrieve 結果のコンテキスト注入方式（`<ltm-episodes>` セクション）
- retrieve 頻度とコスト（embedding API 呼び出し）のバランス
- システムプロンプト肥大化の制御

本マイルストーンは M14a-d の運用結果を見て着手判断する。

## 4. 実施順序

1. ~~`M14b` ファクト注入の関連性フィルタ~~ 完了
2. `M13e` 正式アカウントログイン設計
3. `M14c` 記憶システム責務統合
4. `M14d` Embedding 堅牢化
5. `M13f` Discord 連携高度化
6. `M13g` 観測性・評価設計
7. `M14e` RAG パターン導入（将来検討）

並行可能:

- `M13e` と `M13f` の一部調査
- `M14c` と `M14d`

依存上の注意:

- 正式アカウントログインは運用事故リスクが高いため、実装前に `RUNBOOK.md` まで含めた運用設計を先に固める。
- `M14c`（記憶統合）は `M14a`/`M14b` の運用結果を見てから着手する（数日の観察期間が必要）。
- `M14e`（RAG）は `M14a`-`M14d` の運用結果を見て着手判断する。

## 5. 横断的な品質計測

テスト品質はマイルストーン横断で継続観測する。正本は `docs/TEST_QUALITY.md`。

### 5.1 現状スナップショット

最新値は CI アーティファクト `artifacts/test-quality/summary.md` を参照。

| 指標           | 値   | 判定 |
| -------------- | ---- | ---- |
| テスト数       | 729  | -    |
| アサーション数 | 1269 | -    |
| 失敗率         | 0.0% | 良好 |
| 実行時間       | 5.2s | 良好 |

### 5.2 残りの改善対象

テストの目的は「壊れた変更を検出し、原因が追いやすく、運用で回し続けられる」こと。
カバレッジ数値を上げること自体は目標にせず、検出力を高める。

1. `DiscordGateway` — discord.js 依存の adapter 層。`isHomeMessage`、`trackEmojiUsage`、`adaptMessage` は private だがロジックを持つ。テスト方針: public API（`onMessage` + `start` で登録した handler）経由で discord.js の `Client` をモックして検証する。ロジックを export するためだけにメソッドを公開しない。
2. `bootstrap.ts` — DI 配線の正しさは統合テストの領域。現時点では深追いしない。

### 5.3 テスト品質の継続運用

- `nr test:quality` の結果を PR ごとに確認する（CI で自動実行済み）
- `nr test:quality:flake` を週次で確認する（CI スケジュール実行済み）
- `lowest_line_coverage_files_top10` から「重要なのにテストがないモジュール」を定期的に拾い上げる
- M13 の各マイルストーンで新規コードを書く際は、対応テストを同時に書く

### 5.4 後続追加対象

`TEST_QUALITY.md` §4 で定義済み。主要なもの:

- `critical_scenario_coverage`（M13g で具体化）
- `escaped_defect_rate`
- `test_rework_cost`

DoD 補足:

- 品質改善系タスクでは `nr test:quality` の結果を確認できる
- 不安定性を疑う変更では `nr test:quality:flake` の結果も確認できる
- テストを増やすだけでなく、遅い/壊れやすい/薄い領域の可視化ができる
