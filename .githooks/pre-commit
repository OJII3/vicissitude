#!/usr/bin/env bash
# DEPS.md を自動再生成して staging に追加する pre-commit hook

# deps:graph が利用可能な場合のみ実行
if command -v nr >/dev/null 2>&1; then
  if nr deps:graph >/dev/null 2>&1; then
    git add docs/DEPS.md src/*/DEPS.md 2>/dev/null
  fi
fi
