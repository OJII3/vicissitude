DISCORD_TOKEN=
VICISSITUDE_CONFIG_PATH=config/default.json

# Optional: override repository root inside containers or non-standard deployments.
# APP_ROOT=/app

# Optional: required only when features.spotify exists in the JSON profile.
# SPOTIFY_CLIENT_ID=
# SPOTIFY_CLIENT_SECRET=
# SPOTIFY_REFRESH_TOKEN=

# Optional: required only when features.genius exists in the JSON profile.
# GENIUS_ACCESS_TOKEN=

# Optional: required only when features.githubIssues exists in the JSON profile.
# GITHUB_TOKEN=
# GITHUB_OWNER=
# GITHUB_REPO=
